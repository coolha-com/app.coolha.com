'use client'

import { useTranslations } from 'next-intl'

export default function AgentRolePage() {
  const t = useTranslations()
  return (
    <div className=''>
     在 ERC-8004 注册表上发现并探索 Agent Role
    </div>
  )
}
